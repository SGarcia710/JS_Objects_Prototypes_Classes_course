// Math object
"use strict";
(function () {
  // all Math's  methods and properties are static, so we dont need to create a Math Instance, we can use it directly.

  console.log(Math.PI);
  console.log(Math.max(2, 42, 29));
  console.log(Math.round(29.6));
  console.log(Math.floor(29.6));
})();
